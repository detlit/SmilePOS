<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        android:extractNativeLibs="true" — cloudflared ถูกฝังมาในรูป lib/<abi>/libcloudflared.so
        ซึ่งเป็น executable ไม่ใช่ shared library จริง ต้องบังคับให้ตัวติดตั้งแตกไฟล์ออกมาไว้ที่
        nativeLibraryDir ก่อน ถึงจะ execve() ได้ (ตั้งแต่ Android 10 ระบบห้ามรันไฟล์ที่อยู่ใน
        โฟลเดอร์ข้อมูลของแอปเอง จึงไม่มีทางอื่นนอกจากทางนี้)
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:extractNativeLibs="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!--
            เปิดระบบขายให้เข้าถึงจากนอกร้านผ่าน Cloudflare Tunnel (ดู tunnel/TunnelService.java)

            specialUse ไม่ใช่ dataSync เพราะตั้งแต่ Android 15 dataSync ถูกจำกัด 6 ชั่วโมงต่อวัน
            ซึ่งสั้นกว่าเวลาเปิดร้าน พอครบเวลาระบบจะฆ่า service แล้วลิงก์ตายกลางวันโดยไม่มีสัญญาณเตือน
        -->
        <service
            android:name=".tunnel.TunnelService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="ระบบขายหน้าร้านทำหน้าที่เป็น server ให้เจ้าของร้านเข้าถึงจากภายนอกได้ตลอดเวลาทำการ" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- เปิดให้เข้าถึงจากนอกร้าน: service ต้องอยู่ต่อแม้ปิดจอ และต้องกัน Wi-Fi/CPU หลับ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Android 13+ ไม่แสดงแถบสถานะของ foreground service ถ้าไม่ได้รับสิทธิ์นี้ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- สแกนบาร์โค้ดสินค้าด้วยกล้อง (ML Kit) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- พิมพ์ใบเสร็จผ่านเครื่องพิมพ์ Bluetooth (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />

    <!-- Bluetooth บน Android 11 และต่ำกว่า -->
    <uses-permission
        android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- แท็บเล็ตบางรุ่นไม่มีกล้องหลัง ยังให้ติดตั้งได้ (ใช้เครื่องยิงบาร์โค้ดแทน) -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.bluetooth"
        android:required="false" />
</manifest>
